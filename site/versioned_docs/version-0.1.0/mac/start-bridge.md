---
title: Bridge を開始する
description: iPhone から接続できる状態にするために Bridge を開始するページです。
---

Bridge は、iPhone と Mac 上の Codex をつなぐ窓口です。Project を登録したら、次は Bridge を開始します。

## 開始方法

開始方法は 2 つあります。

- `一般` タブで `Bridge を有効にする` をオンにする
- メニューバーから `Bridge を開始` を選ぶ

## 正常に始まったかを確認する

次の状態になっていれば準備完了です。

- `ランタイム` が `実行中`
- `ヘルス` が表示される
- `ペアリング` タブで QR を表示できる
- `Bridge` タブにローカル endpoint が表示される

## 変更が必要になることがある項目

通常は初期値のままで構いませんが、必要なら `Bridge` タブで次を変えられます。

- 表示名
- ポート
- Pairing 用メモ

変更後は `保存` を押して状態を更新してください。

## うまく始まらないとき

- `詳細` タブで `Codex CLI` と `シェル` の状態を確認する
- `ログ` タブで stdout / stderr を確認する
- 古い launchd 設定が残っているときは `一般` や `詳細` の警告を確認する

> TODO(画像): `一般` タブで `ランタイム: 実行中` が見えている状態と、`ペアリング` タブで QR が表示されている状態の 2 枚。Bridge 開始後に何を確認すればよいかが伝わりやすくなります。
